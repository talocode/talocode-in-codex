{
  "name": "talocode-in-codex",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Codex plugin — Tera, Codra, Skills, ClipLoop, Tradia inside Codex / OpenCode / Claude Code",
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
